#include "mazeSolver.h"

mazeSolver::mazeSolver(mazeGenerator& m_generator) :m_generator(m_generator)
{

    // access the maze data structure through the mazeGenerator object
    // std::vector<mazeGenerator::MazeNode*>& nodes = m_maze.MazeNodes;
    // rest of the solving logic
    // ...
}
